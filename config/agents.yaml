# =============================================================================
# Unified Agent Parameters Configuration
# =============================================================================
# This file serves as the SINGLE source of truth for temperature and max_tokens
# settings across all agent modules. DO NOT hardcode these values elsewhere.
#
# Each module shares ONE set of parameters for all its agents.
# =============================================================================

# Solve Module - Problem solving agents
# Agents: planner_agent, solver_agent, writer_agent
solve:
  temperature: 0.3
  max_tokens: 8192

# Research Module - Deep research agents
# Agents: rephrase_agent, decompose_agent, manager_agent, research_agent, note_agent, reporting_agent
research:
  temperature: 0.5
  max_tokens: 12000

# Question Module - Question generation agents
# Agents: idea_agent, evaluator, generator, validator
question:
  temperature: 0.7
  max_tokens: 4096

# Guide Module - Learning guidance agents
# Agents: design_agent, interactive_agent, chat_agent, summary_agent
guide:
  temperature: 0.5
  max_tokens: 16192

# Brainstorm Tool - Single-call breadth-first idea exploration
brainstorm:
  temperature: 0.8
  max_tokens: 2048

# CoWriter Module - Collaborative writing agents
# Agents: edit_agent
co_writer:
  temperature: 0.7
  max_tokens: 4096

# Personalization Module - Memory agents (reflection, summary, weakness)
personalization:
  temperature: 0.5
  max_tokens: 8192

# =============================================================================
# Narrator Agent - Independent configuration for TTS integration
# =============================================================================
# This agent has special requirements for TTS (Text-to-Speech) integration:
# - max_tokens is limited to 4000 due to OpenAI TTS API character limits
# - Separate configuration allows fine-tuning without affecting other agents
narrator:
  temperature: 0.7
  max_tokens: 4000

# =============================================================================
# Vision Solver Module - Image analysis and GeoGebra visualization
# =============================================================================
# Agents: vision_solver_agent (with bbox, analysis, ggbscript, reflection stages)
# This module handles:
# - BBox: Visual element detection with pixel coordinates
# - Analysis: Geometric semantic analysis
# - GGBScript: GeoGebra command generation
# - Reflection: Command validation and correction
vision_solver:
  temperature: 0.3
  max_tokens: 12000

# Math Animator Module - Manim generation and repair stages
# Agents: concept_analysis_agent, concept_design_agent, code_generator_agent, summary_agent
math_animator:
  temperature: 0.4
  max_tokens: 12000
