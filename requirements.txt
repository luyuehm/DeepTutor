# ============================================
# DeepTutor Python Dependencies (Full Install)
# ============================================
# Python >= 3.10 required
#
# For lighter installs, use the layered requirements:
#   pip install -r requirements/core.txt        # CLI only (~80MB)
#   pip install -r requirements/server.txt      # + API server
#   pip install -r requirements/rag-lite.txt    # + lightweight RAG
#   pip install -r requirements/rag-full.txt    # + all RAG providers
#   pip install -r requirements/providers.txt   # + native LLM SDKs
#   pip install -r requirements/dev.txt         # + dev/test tools

-r requirements/server.txt
-r requirements/rag-full.txt
-r requirements/providers.txt

# ============================================
# Development and testing tools
# ============================================
pytest>=7.0.0
pre-commit>=3.0.0
safety<3.0.0
bandit>=1.8.0
