# -*- coding: utf-8 -*-
"""
Learning Memory (stub)
======================

The legacy ``LearningMemory`` class that managed ``MEMORY.md`` has been
removed.  Memory is now handled entirely by the trace-forest system
(``memory.md``, ``weakness.md``, ``reflection.md``) and the three
ReAct memory agents.

This module is kept as an empty stub so that any stale import paths
fail loudly with an ``ImportError`` rather than silently.
"""
