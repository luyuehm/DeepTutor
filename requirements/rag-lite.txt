# ============================================
# DeepTutor Lightweight RAG Dependencies
# ============================================
# Install: pip install -r requirements/rag-lite.txt
# Add-on layer for LlamaIndex-only RAG + PDF parsing.
# Use with core/server (e.g., `core + rag-lite` or `server + rag-lite`).

llama-index>=0.14.12
PyMuPDF>=1.26.0
numpy>=1.24.0,<2.0.0
arxiv>=2.0.0
