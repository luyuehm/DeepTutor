# ============================================
# DeepTutor Optional LLM Provider SDKs
# ============================================
# Install: pip install -r requirements/providers.txt
# Optional add-on layer for native provider SDKs.
# Skip this if you only use OpenAI-compatible endpoints.

anthropic>=0.30.0
dashscope>=1.14.0
perplexityai>=0.1.0
oauth-cli-kit>=0.2.0
